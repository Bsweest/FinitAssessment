Use Visual Studio or any C# code editor to run Server project.
Make sure to change ConnectionStrings:Default to your MSSQL server connection string in appsettings[.Development].json .</br>
When starting, the application will auto create database and seed data.
If the frontend is not working, maybe `npm install` first.
